** Can we run fosterquest while having the netdrive.sys loaded? **
This would make development a lot easier (copy over fosquest.com then run it immediately)

Need to figure out 1) where netdrive.sys is loaded into memory, 2) what memory fosquest.com is
taking up and 3) if we can prevent them from overlapping.

fosquest.com *should* load at 1920h and take up about 1600 bytes. Memory map:
        0h -  1920h: ??
     1920h -  15fch: fosquest.com
     15fch -     ? : free?
         ? -  ae00h: stack (growing downward)
     ae00h - 11700h: background buf
    11700h - 18000h: compositor buf
    18000h - 1fd00h: framebuffer (video memory)
    1fd00h - 20000h: ??

Can we check these areas out on the PCjr to see if they're accurate?

Then reboot with netdrive.sys installed and check them again

Use this (https://jacobfilipp.com/DrDobbs/articles/DDJ/1988/8811/8811e/8811e.htm) to see if we can
get a better memory map

maybe use MCBs (allocate memory (function 48H), free allocated memory (function 49H), and modify allocated memory (function 4AH).)
to reserve memory?